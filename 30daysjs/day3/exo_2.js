// Write a script that prompt the user to enter base and height of the triangle and calculate an area of a triangle (area = 0.5 x b x h).
/*
    let b = +prompt("Enter base here");
    let h = +prompt("Enter heigh here");
    let area = b * h * 0.5
    console.log(area);
*/

// Write a script that prompt the user to enter side a, side b, and side c of the triangle and and calculate the perimeter of triangle (perimeter = a + b + c)
/*
    let a = +prompt("Enter a here");
    let b = +prompt("Enter b here");
    let c = +prompt("Enter c here");
    let perimeter = a + b + c;
    console.log(perimeter);
*/

// Get length and width using prompt and calculate an area of rectangle (area = length x width and the perimeter of rectangle (perimeter = 2 x (length + width))
/*
    // + refer to type casting (convert string number to int we can also use parseInt)
    let l = +prompt("Enter length here") 
    let w = +prompt("Enter width here")
    let area = l * w;
    let perimeter = 2 * (l + w);
    console.log(`The area is ${l}\nThe perimeter is: ${perimeter}`);
*/

// Get radius using prompt and calculate the area of a circle (area = pi x r x r) and circumference of a circle(c = 2 x pi x r) where pi = 3.14.
// can be done

// Calculate the slope, x-intercept and y-intercept of y = 2x -2 [y = ax + b]
/*
    let a = +prompt("a")
    let b = +prompt("b")
    let x = -b / a
    let y = a * x + b
    console.log(`y = ${y}`);
*/

// Slope is m = (y2-y1)/(x2-x1). Find the slope between point (2, 2) and point(6,10)
// can be done

// Compare the slope of above two questions.
// can be done

// Calculate the value of y (y = x2 +longd find appropriate x value

// Writ a script that prompt a user to enter hours and rate per hour. Calculate pay of the person?
// can be done

// If the length of your name is greater than 7 say, your name is long else say your name is short.
/*
    let name = prompt("Enter yout name")
    let nameLength = name.length

    nameLength > 7 
    ? console.log(`Your name is ${nameLength} char long`) 
    : console.log(`Your name is ${nameLength} char short`)
*/

// Compare your first name length and your family name length and you should get this output.
// Your first name, Asabeneh is longer than your family name, Yetayeh
/*
    let firstName = prompt("Enter your first name");
    let familyName = prompt('Enter your family Name');
    firstNameLength = firstName.length;
    familyNameLength = familyName.length;

    firstNameLength > familyNameLength
    ? console.log(`Your first Name, ${firstName} is longer than your family name, ${familyName}`)
    :  console.log(`Your first Name, ${firstName} is shorter than your family name, ${familyName}`)
*/

// Declare two variables myAge and yourAge and assign them initial values and myAge and yourAge.
// can be done 

// Using prompt get the year the user was born and if the user is 18 or above allow the user to drive if not tell the user to wait a certain amount of years.
// can be done

// Write a script that prompt the user to enter number of years. Calculate the number of seconds a person can live. Assume some one lives just hundred years
// can be done 

// Create a human readable time format using the Date time object
/*
    const date1 = new Date();
    let year = date1.getFullYear();
    let month = date1.getMonth() + 1;
    let day = date1.getDate();
    let hour = date1.getHours()
    let min = date1.getMinutes();
    // YYYY-MM-DD HH:mm
    console.log(`${year}-${month}-${day} ${hour}:${min}`);
*/

// DD-MM-YYYY HH:mm
// Do same as above and change place of things

// DD/MM/YYYY HH:mm
// Do same as above and change place of things

